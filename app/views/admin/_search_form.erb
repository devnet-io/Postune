<%= form_for(admin_index_path, :method => 'get', :id => 'admin_search') do %>
	<div class="field">
		<%= text_field_tag :search, params[:search] %>
	</div>
	<div class="action">
		<%= submit_tag 'Search' %>
	</div>
<% end %>	
