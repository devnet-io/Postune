<%= form_for @new_user, :url => url do |f| %>
	<%= render 'shared/form_error_messages', :object => f.object %>
	<div class="field">
		<%= f.label :name %>
		<%= f.text_field :name %>
	</div>
	<div class="field">
		<%= f.label :email %>
		<%= f.text_field :email %>
	</div>
	<div class="field">
		<%= f.label :unencrypted_password, 'Password' %>
		<%= f.password_field :unencrypted_password %>
	</div>
	<div class="field">
		<%= f.label :unencrypted_password_confirmation, 'Confirm Password' %>
		<%= f.password_field :unencrypted_password_confirmation %>
	</div>
	<div class="actions">
		<%= f.submit "Register" %>
	</div>
<% end %>
